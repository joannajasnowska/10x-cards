---
// Redirect to generator page - this will trigger the authentication middleware
// which will either allow access or redirect to login if not authenticated
export const prerender = false;

// Perform immediate redirect to generator
return Astro.redirect("/generator");
---
